<%= form_with(model: @question) do |f| %>

  <%= f.label :content %>
  <%= f.text_field :content, autofocus: true %>

  <%= f.label :topic %>
  <%= f.select :topic, Question::TOPIC, prompt: "Pick one" %>

  <%= f.submit %>

<% end %>



